/** Single source of truth for product naming, so a rename is a one-line change. */
export const BRAND = {
  name: 'Wonderverse',
  tagline: 'Step inside learning.',
  storageKey: 'wonderverse:v1',
} as const;
